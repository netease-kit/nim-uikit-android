<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item  android:drawable="@android:color/darker_gray" android:id="@+id/background" />    <!-- 前景 -->
    <item android:id="@+id/foreground">
        <clip>
            <shape android:shape="rectangle">
                <solid android:color="@android:color/holo_blue_light" /> <!-- 前景颜色 -->
            </shape>
        </clip>
    </item>
</layer-list>